import 'package:flutter/material.dart';

class Libarian extends StatelessWidget {
  const Libarian({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Libarian');
  }
}