import 'package:flutter/material.dart';

class AddEditFees extends StatelessWidget {
  const AddEditFees({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('AddEditFees');
  }
}