import 'package:flutter/material.dart';

class AllExams extends StatelessWidget {
  const AllExams({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('AllExams');
  }
}