{
    "version": 2,
    "buildCommand": "export PATH=\"$PATH:$PWD/flutter/bin\" && flutter build web --release",
    "outputDirectory": "build/web",
    "installCommand": "curl -o flutter_linux.tar.xz https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.5-stable.tar.xz && tar xf flutter_linux.tar.xz && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter doctor && flutter clean && flutter config --enable-web && flutter pub get"
}